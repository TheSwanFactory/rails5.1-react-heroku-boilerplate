{
  "name": "MyRailsReactApp",
  "description": "Run MyRailsReactApp on Heroku",
  "addons": ["heroku-postgresql:hobby-dev"],
  "env": {
    "RAILS_ENV": "production"
  },
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate"
  }
}
