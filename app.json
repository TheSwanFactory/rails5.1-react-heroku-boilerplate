{
  "name": "rails-5.1-react-heroku-boilerplate",
  "description": "Template for running Rails 5.1 and React on Heroku",
  "addons": ["heroku-postgresql:hobby-dev"],
  "env": {
    "RAILS_ENV": "production"
  },
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate"
  }
}
